# 大规模纯格式提交清单 —— 这些提交不含任何实质内容改动，应从 blame 中排除。
#
# GitHub 网页版 blame 自动识别本文件；本地需启用一次（本地配置，不入库，换机器要重跑）：
#   git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# 加入条件：提交必须是纯格式/纯行尾变化，且能用
#   git show --ignore-cr-at-eol <rev> --numstat
# 之类的方式证明实质差异为零。有任何逻辑改动的提交不得列入。

# chore(repo): 按 .gitattributes 一次性重归一化行尾（254 个文件，2026-07-29）
7a6d873753e01ad68f0be0daa9fa5acb770faf64
